# Pasos para iniciar la app
se debe activar un entorno virtual para iniciar la app
1. ejecutamos el comando ```pip install virtualenv```
2. ejecutamos el comando ```virtualenv venv```
3. activamos el entorno con el comando ```source venv/bin/activate```
4. una vez activo vamos a instalar las dependencias con el comando ```pip install -r requirements.txt```

